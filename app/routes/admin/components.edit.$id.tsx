import {json, LoaderArgs} from "@remix-run/node";

export const loadder = async ({ request }: LoaderArgs) => {
    const component =

    return json({ component });
};

export default function Edit()
{

}
